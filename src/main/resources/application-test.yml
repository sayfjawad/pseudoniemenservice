server:
  compression:
    enabled: true
  forward-headers-strategy: framework
logging:
  level:
    ROOT: info
    nl.ictu: debug
spring:
  application:
    name: Pseudoniemen Service
management:
  endpoints:
    enabled-by-default: true
    web:
      exposure:
        include: '*'

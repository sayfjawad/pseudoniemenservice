server:
  compression:
    enabled: true
  forward-headers-strategy: framework
logging:
  level:
    ROOT: info
    ### waarom is debug altijd aan op productie terwijl er bijna geen logging is voor die packages?
    nl.ictu: debug
spring:
  application:
    name: Pseudoniemen Service
management:
  endpoint:
    info:
      access: read_only
    health:
      access: read_only
      probes:
        enabled: true